const payment = require('../models/payModel'); 

module.exports+{
    async all(req, res){
        try{

        } catch (error) {
            console.log('error log: ', error)
        }
    }
}